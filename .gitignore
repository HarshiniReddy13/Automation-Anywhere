node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.env
*.log
dist/
# Saved by global-setup.ts; contains a live session token, not a secret to commit.
.auth/
# Self-contained HTML reports generated per execution by CustomHtmlReporter;
# can embed large base64 video/screenshots, and a new file is written every run.
reports/
# Runtime checkpoint state for the API automation module (context/CheckpointManager.ts) — regenerated every run.
.checkpoints/
.DS_Store
**/.DS_Store
_l.mjs
_launch.mjs_l.mjs
_launch.mjs
